/**
 * App version shown in Settings → About.
 * Keep in sync with `android/app/build.gradle` `versionName`.
 * (No DeviceInfo dependency — avoid native module churn for display-only.)
 */
export const APP_VERSION_NAME = '1.0.1';
