package com.homeservices.providerapp

import android.app.Application

/** No-op in release — Flipper is not bundled. */
object FlipperInitializer {
  fun init(application: Application) {
    // intentionally empty
  }
}
